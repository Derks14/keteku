// import { useContext } from "react";
// import { ThemeContext, ThemeContextType } from "@/components/context/theme-context.tsx";
//
// export const useTheme = (): ThemeContextType => {
//   const context = useContext(ThemeContext);
//   if (!context) throw new Error("use context must be use in a provider")
//   return context;
// }
